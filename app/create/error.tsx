'use client';

import { ErrorScreen, type ErrorScreenProps } from '@/components/global/ErrorScreen';

export default function CreateError(props: ErrorScreenProps) {
  return <ErrorScreen {...props} />;
}
