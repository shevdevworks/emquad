/**
 * The sixteen gallery manifests, as /create addresses copied from the
 * browser's address bar. Checked by `npm run gallery:check` before any of
 * them are written to the database (stage 7.3).
 */
export const GALLERY_URLS: readonly string[] = [];
